# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth/login.spec.ts >> Login Page >> should show form validation for empty password
- Location: tests/e2e/auth/login.spec.ts:87:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByPlaceholder('Enter your password')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByPlaceholder('Enter your password')

```

# Page snapshot

```yaml
- generic [ref=e2]:
  - generic [ref=e4]:
    - generic [ref=e5]:
      - generic [ref=e6]:
        - img [ref=e8]
        - heading "Sign In" [level=2] [ref=e12]
        - paragraph [ref=e13]: Enter your username to continue
      - separator [ref=e14]
      - generic [ref=e16]:
        - generic [ref=e17]:
          - generic [ref=e18]: Username
          - generic [ref=e19]:
            - img [ref=e21]
            - textbox "Username" [ref=e24]:
              - /placeholder: Enter your username
              - text: admin
        - button "Next" [active] [ref=e26] [cursor=pointer]:
          - generic [ref=e28]: Next
      - paragraph [ref=e29]:
        - text: Copyright ©
        - link "Carmen Software Co.,Ltd." [ref=e30] [cursor=pointer]:
          - /url: https://carmensoftware.com/
        - text: "2026."
    - paragraph [ref=e32]:
      - text: 🔧 Developer Mode
      - text: "Default: Tenant=dev, UserName=admin, Password=alpha"
  - generic [ref=e33]:
    - img [ref=e35]
    - button "Open Tanstack query devtools" [ref=e83] [cursor=pointer]:
      - img [ref=e84]
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | /**
  4   |  * Authentication Login Flow Tests
  5   |  * 
  6   |  * These tests verify:
  7   |  * - Login page loads correctly
  8   |  * - Successful login redirects to dashboard
  9   |  * - Failed login shows error message
  10  |  * - Form validation (required fields)
  11  |  * - Redirect to originally requested page after login
  12  |  */
  13  | 
  14  | // Helper function to perform complete login
  15  | async function performLogin(page: any, username: string = 'admin', password: string = 'alpha') {
  16  |   // Step 1: Enter username and click Next
  17  |   const usernameField = page.getByPlaceholder('Enter your username');
  18  |   await usernameField.fill(username);
  19  |   
  20  |   const nextButton = page.getByRole('button', { name: 'Next' });
  21  |   await nextButton.click();
  22  |   
  23  |   // Wait for password field to appear
  24  |   await page.waitForSelector('input[type="password"]', { timeout: 5000 });
  25  |   
  26  |   // Step 2: Enter password and click Sign In
  27  |   const passwordField = page.getByPlaceholder('Enter your password');
  28  |   await passwordField.fill(password);
  29  |   
  30  |   const signInButton = page.getByRole('button', { name: 'Sign In' });
  31  |   await signInButton.click();
  32  | }
  33  | 
  34  | test.describe('Login Page', () => {
  35  |   test.beforeEach(async ({ page }) => {
  36  |     // Clear any existing auth state - navigate first, then clear
  37  |     await page.goto('/login');
  38  |     await page.evaluate(() => {
  39  |       localStorage.clear();
  40  |       sessionStorage.clear();
  41  |     });
  42  |     await page.reload();
  43  |   });
  44  | 
  45  |   test('should display login page correctly', async ({ page }) => {
  46  |     // Check page title
  47  |     await expect(page).toHaveTitle(/Carmen/i);
  48  |     
  49  |     // Check login form elements are visible (Step 1 - Username)
  50  |     await expect(page.getByRole('heading', { name: 'Sign In' })).toBeVisible();
  51  |     await expect(page.getByText('Enter your username to continue')).toBeVisible();
  52  |     
  53  |     const usernameField = page.getByPlaceholder('Enter your username');
  54  |     const nextButton = page.getByRole('button', { name: 'Next' });
  55  |     
  56  |     await expect(usernameField).toBeVisible();
  57  |     await expect(nextButton).toBeVisible();
  58  |     
  59  |     // Check for developer mode hint
  60  |     await expect(page.getByText('Developer Mode')).toBeVisible();
  61  |   });
  62  | 
  63  |   test('should show password field after entering username', async ({ page }) => {
  64  |     // Step 1: Enter username
  65  |     await page.getByPlaceholder('Enter your username').fill('admin');
  66  |     await page.getByRole('button', { name: 'Next' }).click();
  67  |     
  68  |     // Step 2: Password field should appear
  69  |     await expect(page.getByPlaceholder('Enter your password')).toBeVisible({ timeout: 5000 });
  70  |     await expect(page.getByText('Enter your password')).toBeVisible();
  71  |     
  72  |     const signInButton = page.getByRole('button', { name: 'Sign In' });
  73  |     await expect(signInButton).toBeVisible();
  74  |   });
  75  | 
  76  |   test('should show form validation for empty username', async ({ page }) => {
  77  |     // Click Next without entering username
  78  |     await page.getByRole('button', { name: 'Next' }).click();
  79  |     
  80  |     // Should remain on login page
  81  |     await expect(page).toHaveURL(/\/login/);
  82  |     
  83  |     // Should show validation error
  84  |     await expect(page.getByText('Username is required')).toBeVisible({ timeout: 5000 });
  85  |   });
  86  | 
  87  |   test('should show form validation for empty password', async ({ page }) => {
  88  |     // Step 1: Enter username
  89  |     await page.getByPlaceholder('Enter your username').fill('admin');
  90  |     await page.getByRole('button', { name: 'Next' }).click();
  91  |     
  92  |     // Wait for password field
> 93  |     await expect(page.getByPlaceholder('Enter your password')).toBeVisible({ timeout: 5000 });
      |                                                                ^ Error: expect(locator).toBeVisible() failed
  94  |     
  95  |     // Click Sign In without entering password
  96  |     await page.getByRole('button', { name: 'Sign In' }).click();
  97  |     
  98  |     // Should show validation error
  99  |     await expect(page.getByText('Password is required')).toBeVisible({ timeout: 5000 });
  100 |   });
  101 | });
  102 | 
  103 | test.describe('Login Flow - Successful Authentication', () => {
  104 |   test.beforeEach(async ({ page }) => {
  105 |     await page.goto('/login');
  106 |     await page.evaluate(() => {
  107 |       localStorage.clear();
  108 |       sessionStorage.clear();
  109 |     });
  110 |     await page.reload();
  111 |   });
  112 | 
  113 |   test('should login successfully with valid credentials', async ({ page }) => {
  114 |     // Perform two-step login
  115 |     await performLogin(page, 'admin', 'alpha');
  116 |     
  117 |     // Wait for navigation to dashboard
  118 |     await page.waitForURL(/\/(dashboard|home|$)/, { timeout: 10000 });
  119 |     
  120 |     // Verify we're logged in (dashboard visible)
  121 |     await expect(page.locator('body')).toContainText(/Dashboard|Carmen/i);
  122 |     
  123 |     // Verify token is stored
  124 |     const token = await page.evaluate(() => localStorage.getItem('AccessToken'));
  125 |     expect(token).toBeTruthy();
  126 |   });
  127 | 
  128 |   test('should redirect to originally requested page after login', async ({ page }) => {
  129 |     // Try to access a protected page directly
  130 |     const targetUrl = '/ar/folio';
  131 |     await page.goto(targetUrl);
  132 |     
  133 |     // Should be redirected to login
  134 |     await expect(page).toHaveURL(/\/login/);
  135 |     
  136 |     // Perform login
  137 |     await performLogin(page, 'admin', 'alpha');
  138 |     
  139 |     // Should redirect to originally requested page
  140 |     await page.waitForURL(/\/ar\/folio/, { timeout: 10000 });
  141 |     await expect(page).toHaveURL(/\/ar\/folio/);
  142 |   });
  143 | });
  144 | 
  145 | test.describe('Login Flow - Failed Authentication', () => {
  146 |   test.beforeEach(async ({ page }) => {
  147 |     await page.goto('/login');
  148 |     await page.evaluate(() => {
  149 |       localStorage.clear();
  150 |       sessionStorage.clear();
  151 |     });
  152 |     await page.reload();
  153 |   });
  154 | 
  155 |   test('should show error for invalid password', async ({ page }) => {
  156 |     // Step 1: Enter username
  157 |     await page.getByPlaceholder('Enter your username').fill('admin');
  158 |     await page.getByRole('button', { name: 'Next' }).click();
  159 |     
  160 |     // Wait for password field
  161 |     await expect(page.getByPlaceholder('Enter your password')).toBeVisible({ timeout: 5000 });
  162 |     
  163 |     // Step 2: Enter wrong password
  164 |     await page.getByPlaceholder('Enter your password').fill('wrongpassword');
  165 |     await page.getByRole('button', { name: 'Sign In' }).click();
  166 |     
  167 |     // Should show error notification
  168 |     await expect(page.locator('.mantine-Notification-root')).toBeVisible({ timeout: 5000 });
  169 |     
  170 |     // Should remain on login page (password step)
  171 |     await expect(page.getByPlaceholder('Enter your password')).toBeVisible();
  172 |   });
  173 | 
  174 |   test('should show error for non-existent username', async ({ page }) => {
  175 |     // Enter non-existent username
  176 |     await page.getByPlaceholder('Enter your username').fill('nonexistentuser12345');
  177 |     await page.getByRole('button', { name: 'Next' }).click();
  178 |     
  179 |     // Should show error notification
  180 |     await expect(page.locator('.mantine-Notification-root')).toBeVisible({ timeout: 5000 });
  181 |     
  182 |     // Should remain on login page
  183 |     await expect(page).toHaveURL(/\/login/);
  184 |   });
  185 | 
  186 |   test('should clear password field after failed login', async ({ page }) => {
  187 |     // Step 1: Enter username
  188 |     await page.getByPlaceholder('Enter your username').fill('admin');
  189 |     await page.getByRole('button', { name: 'Next' }).click();
  190 |     
  191 |     // Wait for password field
  192 |     await expect(page.getByPlaceholder('Enter your password')).toBeVisible({ timeout: 5000 });
  193 |     
```